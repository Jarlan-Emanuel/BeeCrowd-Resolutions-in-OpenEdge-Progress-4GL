DEFINE VARIABLE pi AS DECIMAL NO-UNDO.
DEFINE VARIABLE conta AS DECIMAL NO-UNDO.
DEFINE VARIABLE resultado AS DECIMAL NO-UNDO.

conta = 100.64.
pi = 3.14159.
resultado = pi * (conta * conta) .


DISPLAY "A = "resultado.